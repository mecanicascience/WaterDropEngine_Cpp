#pragma once

#include <WaterDropEngine/includes.hpp>

using namespace wde;
class ProjectsApp {
	public:
		/** Create a new application */
		ProjectsApp() = default;

		/** Starts the application */
		void start();
};
