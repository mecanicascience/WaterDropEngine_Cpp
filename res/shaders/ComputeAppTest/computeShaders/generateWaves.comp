#version 450

// Size of each group (use groups of size 16*16*1)
layout(local_size_x = 16, local_size_y = 16, local_size_z = 1) in;

// The buffer to write to
layout(std140, set = 2, binding = 0) writeonly buffer ObjectBuffer {
    int defaultVal;
} objectBuffer;

void main() {
    objectBuffer.defaultVal = 0;
}
